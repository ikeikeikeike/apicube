settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: rpc
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    - ../base
    ignored_paths:
    - .git
    - .realize
    - vendor
