// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// DTBTaxRule is an object representing the database table.
type DTBTaxRule struct {
	ID                uint          `boil:"id" json:"id" toml:"id" yaml:"id"`
	ProductClassID    null.Uint     `boil:"product_class_id" json:"product_class_id,omitempty" toml:"product_class_id" yaml:"product_class_id,omitempty"`
	CreatorID         null.Uint     `boil:"creator_id" json:"creator_id,omitempty" toml:"creator_id" yaml:"creator_id,omitempty"`
	CountryID         null.Uint16   `boil:"country_id" json:"country_id,omitempty" toml:"country_id" yaml:"country_id,omitempty"`
	PrefID            null.Uint16   `boil:"pref_id" json:"pref_id,omitempty" toml:"pref_id" yaml:"pref_id,omitempty"`
	ProductID         null.Uint     `boil:"product_id" json:"product_id,omitempty" toml:"product_id" yaml:"product_id,omitempty"`
	RoundingTypeID    null.Uint16   `boil:"rounding_type_id" json:"rounding_type_id,omitempty" toml:"rounding_type_id" yaml:"rounding_type_id,omitempty"`
	TaxRate           types.Decimal `boil:"tax_rate" json:"tax_rate" toml:"tax_rate" yaml:"tax_rate"`
	TaxAdjust         types.Decimal `boil:"tax_adjust" json:"tax_adjust" toml:"tax_adjust" yaml:"tax_adjust"`
	ApplyDate         time.Time     `boil:"apply_date" json:"apply_date" toml:"apply_date" yaml:"apply_date"`
	CreateDate        time.Time     `boil:"create_date" json:"create_date" toml:"create_date" yaml:"create_date"`
	UpdateDate        time.Time     `boil:"update_date" json:"update_date" toml:"update_date" yaml:"update_date"`
	DiscriminatorType string        `boil:"discriminator_type" json:"discriminator_type" toml:"discriminator_type" yaml:"discriminator_type"`

	R *dtbTaxRuleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dtbTaxRuleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DTBTaxRuleColumns = struct {
	ID                string
	ProductClassID    string
	CreatorID         string
	CountryID         string
	PrefID            string
	ProductID         string
	RoundingTypeID    string
	TaxRate           string
	TaxAdjust         string
	ApplyDate         string
	CreateDate        string
	UpdateDate        string
	DiscriminatorType string
}{
	ID:                "id",
	ProductClassID:    "product_class_id",
	CreatorID:         "creator_id",
	CountryID:         "country_id",
	PrefID:            "pref_id",
	ProductID:         "product_id",
	RoundingTypeID:    "rounding_type_id",
	TaxRate:           "tax_rate",
	TaxAdjust:         "tax_adjust",
	ApplyDate:         "apply_date",
	CreateDate:        "create_date",
	UpdateDate:        "update_date",
	DiscriminatorType: "discriminator_type",
}

// Generated where

var DTBTaxRuleWhere = struct {
	ID                whereHelperuint
	ProductClassID    whereHelpernull_Uint
	CreatorID         whereHelpernull_Uint
	CountryID         whereHelpernull_Uint16
	PrefID            whereHelpernull_Uint16
	ProductID         whereHelpernull_Uint
	RoundingTypeID    whereHelpernull_Uint16
	TaxRate           whereHelpertypes_Decimal
	TaxAdjust         whereHelpertypes_Decimal
	ApplyDate         whereHelpertime_Time
	CreateDate        whereHelpertime_Time
	UpdateDate        whereHelpertime_Time
	DiscriminatorType whereHelperstring
}{
	ID:                whereHelperuint{field: `id`},
	ProductClassID:    whereHelpernull_Uint{field: `product_class_id`},
	CreatorID:         whereHelpernull_Uint{field: `creator_id`},
	CountryID:         whereHelpernull_Uint16{field: `country_id`},
	PrefID:            whereHelpernull_Uint16{field: `pref_id`},
	ProductID:         whereHelpernull_Uint{field: `product_id`},
	RoundingTypeID:    whereHelpernull_Uint16{field: `rounding_type_id`},
	TaxRate:           whereHelpertypes_Decimal{field: `tax_rate`},
	TaxAdjust:         whereHelpertypes_Decimal{field: `tax_adjust`},
	ApplyDate:         whereHelpertime_Time{field: `apply_date`},
	CreateDate:        whereHelpertime_Time{field: `create_date`},
	UpdateDate:        whereHelpertime_Time{field: `update_date`},
	DiscriminatorType: whereHelperstring{field: `discriminator_type`},
}

// DTBTaxRuleRels is where relationship names are stored.
var DTBTaxRuleRels = struct {
	RoundingType string
	Product      string
	Pref         string
	Country      string
}{
	RoundingType: "RoundingType",
	Product:      "Product",
	Pref:         "Pref",
	Country:      "Country",
}

// dtbTaxRuleR is where relationships are stored.
type dtbTaxRuleR struct {
	RoundingType *MTBRoundingType
	Product      *DTBProduct
	Pref         *MTBPref
	Country      *MTBCountry
}

// NewStruct creates a new relationship struct
func (*dtbTaxRuleR) NewStruct() *dtbTaxRuleR {
	return &dtbTaxRuleR{}
}

// dtbTaxRuleL is where Load methods for each relationship are stored.
type dtbTaxRuleL struct{}

var (
	dtbTaxRuleColumns               = []string{"id", "product_class_id", "creator_id", "country_id", "pref_id", "product_id", "rounding_type_id", "tax_rate", "tax_adjust", "apply_date", "create_date", "update_date", "discriminator_type"}
	dtbTaxRuleColumnsWithoutDefault = []string{"product_class_id", "creator_id", "country_id", "pref_id", "product_id", "rounding_type_id", "apply_date", "create_date", "update_date", "discriminator_type"}
	dtbTaxRuleColumnsWithDefault    = []string{"id", "tax_rate", "tax_adjust"}
	dtbTaxRulePrimaryKeyColumns     = []string{"id"}
)

type (
	// DTBTaxRuleSlice is an alias for a slice of pointers to DTBTaxRule.
	// This should generally be used opposed to []DTBTaxRule.
	DTBTaxRuleSlice []*DTBTaxRule
	// DTBTaxRuleHook is the signature for custom DTBTaxRule hook methods
	DTBTaxRuleHook func(context.Context, boil.ContextExecutor, *DTBTaxRule) error

	dtbTaxRuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dtbTaxRuleType                 = reflect.TypeOf(&DTBTaxRule{})
	dtbTaxRuleMapping              = queries.MakeStructMapping(dtbTaxRuleType)
	dtbTaxRulePrimaryKeyMapping, _ = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, dtbTaxRulePrimaryKeyColumns)
	dtbTaxRuleInsertCacheMut       sync.RWMutex
	dtbTaxRuleInsertCache          = make(map[string]insertCache)
	dtbTaxRuleUpdateCacheMut       sync.RWMutex
	dtbTaxRuleUpdateCache          = make(map[string]updateCache)
	dtbTaxRuleUpsertCacheMut       sync.RWMutex
	dtbTaxRuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dtbTaxRuleBeforeInsertHooks []DTBTaxRuleHook
var dtbTaxRuleBeforeUpdateHooks []DTBTaxRuleHook
var dtbTaxRuleBeforeDeleteHooks []DTBTaxRuleHook
var dtbTaxRuleBeforeUpsertHooks []DTBTaxRuleHook

var dtbTaxRuleAfterInsertHooks []DTBTaxRuleHook
var dtbTaxRuleAfterSelectHooks []DTBTaxRuleHook
var dtbTaxRuleAfterUpdateHooks []DTBTaxRuleHook
var dtbTaxRuleAfterDeleteHooks []DTBTaxRuleHook
var dtbTaxRuleAfterUpsertHooks []DTBTaxRuleHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DTBTaxRule) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DTBTaxRule) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DTBTaxRule) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DTBTaxRule) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DTBTaxRule) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DTBTaxRule) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DTBTaxRule) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DTBTaxRule) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DTBTaxRule) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbTaxRuleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDTBTaxRuleHook registers your hook function for all future operations.
func AddDTBTaxRuleHook(hookPoint boil.HookPoint, dtbTaxRuleHook DTBTaxRuleHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		dtbTaxRuleBeforeInsertHooks = append(dtbTaxRuleBeforeInsertHooks, dtbTaxRuleHook)
	case boil.BeforeUpdateHook:
		dtbTaxRuleBeforeUpdateHooks = append(dtbTaxRuleBeforeUpdateHooks, dtbTaxRuleHook)
	case boil.BeforeDeleteHook:
		dtbTaxRuleBeforeDeleteHooks = append(dtbTaxRuleBeforeDeleteHooks, dtbTaxRuleHook)
	case boil.BeforeUpsertHook:
		dtbTaxRuleBeforeUpsertHooks = append(dtbTaxRuleBeforeUpsertHooks, dtbTaxRuleHook)
	case boil.AfterInsertHook:
		dtbTaxRuleAfterInsertHooks = append(dtbTaxRuleAfterInsertHooks, dtbTaxRuleHook)
	case boil.AfterSelectHook:
		dtbTaxRuleAfterSelectHooks = append(dtbTaxRuleAfterSelectHooks, dtbTaxRuleHook)
	case boil.AfterUpdateHook:
		dtbTaxRuleAfterUpdateHooks = append(dtbTaxRuleAfterUpdateHooks, dtbTaxRuleHook)
	case boil.AfterDeleteHook:
		dtbTaxRuleAfterDeleteHooks = append(dtbTaxRuleAfterDeleteHooks, dtbTaxRuleHook)
	case boil.AfterUpsertHook:
		dtbTaxRuleAfterUpsertHooks = append(dtbTaxRuleAfterUpsertHooks, dtbTaxRuleHook)
	}
}

// One returns a single dtbTaxRule record from the query.
func (q dtbTaxRuleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DTBTaxRule, error) {
	o := &DTBTaxRule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for dtb_tax_rule")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DTBTaxRule records from the query.
func (q dtbTaxRuleQuery) All(ctx context.Context, exec boil.ContextExecutor) (DTBTaxRuleSlice, error) {
	var o []*DTBTaxRule

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to DTBTaxRule slice")
	}

	if len(dtbTaxRuleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DTBTaxRule records in the query.
func (q dtbTaxRuleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count dtb_tax_rule rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dtbTaxRuleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if dtb_tax_rule exists")
	}

	return count > 0, nil
}

// RoundingType pointed to by the foreign key.
func (o *DTBTaxRule) RoundingType(mods ...qm.QueryMod) mtbRoundingTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.RoundingTypeID),
	}

	queryMods = append(queryMods, mods...)

	query := MTBRoundingTypes(queryMods...)
	queries.SetFrom(query.Query, "`mtb_rounding_type`")

	return query
}

// Product pointed to by the foreign key.
func (o *DTBTaxRule) Product(mods ...qm.QueryMod) dtbProductQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	query := DTBProducts(queryMods...)
	queries.SetFrom(query.Query, "`dtb_product`")

	return query
}

// Pref pointed to by the foreign key.
func (o *DTBTaxRule) Pref(mods ...qm.QueryMod) mtbPrefQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.PrefID),
	}

	queryMods = append(queryMods, mods...)

	query := MTBPrefs(queryMods...)
	queries.SetFrom(query.Query, "`mtb_pref`")

	return query
}

// Country pointed to by the foreign key.
func (o *DTBTaxRule) Country(mods ...qm.QueryMod) mtbCountryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.CountryID),
	}

	queryMods = append(queryMods, mods...)

	query := MTBCountries(queryMods...)
	queries.SetFrom(query.Query, "`mtb_country`")

	return query
}

// LoadRoundingType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbTaxRuleL) LoadRoundingType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBTaxRule interface{}, mods queries.Applicator) error {
	var slice []*DTBTaxRule
	var object *DTBTaxRule

	if singular {
		object = maybeDTBTaxRule.(*DTBTaxRule)
	} else {
		slice = *maybeDTBTaxRule.(*[]*DTBTaxRule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbTaxRuleR{}
		}
		if !queries.IsNil(object.RoundingTypeID) {
			args = append(args, object.RoundingTypeID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbTaxRuleR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.RoundingTypeID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.RoundingTypeID) {
				args = append(args, obj.RoundingTypeID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`mtb_rounding_type`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MTBRoundingType")
	}

	var resultSlice []*MTBRoundingType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MTBRoundingType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mtb_rounding_type")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mtb_rounding_type")
	}

	if len(dtbTaxRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.RoundingType = foreign
		if foreign.R == nil {
			foreign.R = &mtbRoundingTypeR{}
		}
		foreign.R.RoundingTypeDTBTaxRules = append(foreign.R.RoundingTypeDTBTaxRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RoundingTypeID, foreign.ID) {
				local.R.RoundingType = foreign
				if foreign.R == nil {
					foreign.R = &mtbRoundingTypeR{}
				}
				foreign.R.RoundingTypeDTBTaxRules = append(foreign.R.RoundingTypeDTBTaxRules, local)
				break
			}
		}
	}

	return nil
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbTaxRuleL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBTaxRule interface{}, mods queries.Applicator) error {
	var slice []*DTBTaxRule
	var object *DTBTaxRule

	if singular {
		object = maybeDTBTaxRule.(*DTBTaxRule)
	} else {
		slice = *maybeDTBTaxRule.(*[]*DTBTaxRule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbTaxRuleR{}
		}
		if !queries.IsNil(object.ProductID) {
			args = append(args, object.ProductID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbTaxRuleR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ProductID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ProductID) {
				args = append(args, obj.ProductID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dtb_product`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DTBProduct")
	}

	var resultSlice []*DTBProduct
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DTBProduct")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for dtb_product")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for dtb_product")
	}

	if len(dtbTaxRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &dtbProductR{}
		}
		foreign.R.ProductDTBTaxRules = append(foreign.R.ProductDTBTaxRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ProductID, foreign.ID) {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &dtbProductR{}
				}
				foreign.R.ProductDTBTaxRules = append(foreign.R.ProductDTBTaxRules, local)
				break
			}
		}
	}

	return nil
}

// LoadPref allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbTaxRuleL) LoadPref(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBTaxRule interface{}, mods queries.Applicator) error {
	var slice []*DTBTaxRule
	var object *DTBTaxRule

	if singular {
		object = maybeDTBTaxRule.(*DTBTaxRule)
	} else {
		slice = *maybeDTBTaxRule.(*[]*DTBTaxRule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbTaxRuleR{}
		}
		if !queries.IsNil(object.PrefID) {
			args = append(args, object.PrefID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbTaxRuleR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.PrefID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.PrefID) {
				args = append(args, obj.PrefID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`mtb_pref`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MTBPref")
	}

	var resultSlice []*MTBPref
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MTBPref")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mtb_pref")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mtb_pref")
	}

	if len(dtbTaxRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pref = foreign
		if foreign.R == nil {
			foreign.R = &mtbPrefR{}
		}
		foreign.R.PrefDTBTaxRules = append(foreign.R.PrefDTBTaxRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.PrefID, foreign.ID) {
				local.R.Pref = foreign
				if foreign.R == nil {
					foreign.R = &mtbPrefR{}
				}
				foreign.R.PrefDTBTaxRules = append(foreign.R.PrefDTBTaxRules, local)
				break
			}
		}
	}

	return nil
}

// LoadCountry allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbTaxRuleL) LoadCountry(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBTaxRule interface{}, mods queries.Applicator) error {
	var slice []*DTBTaxRule
	var object *DTBTaxRule

	if singular {
		object = maybeDTBTaxRule.(*DTBTaxRule)
	} else {
		slice = *maybeDTBTaxRule.(*[]*DTBTaxRule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbTaxRuleR{}
		}
		if !queries.IsNil(object.CountryID) {
			args = append(args, object.CountryID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbTaxRuleR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.CountryID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.CountryID) {
				args = append(args, obj.CountryID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`mtb_country`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MTBCountry")
	}

	var resultSlice []*MTBCountry
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MTBCountry")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mtb_country")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mtb_country")
	}

	if len(dtbTaxRuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Country = foreign
		if foreign.R == nil {
			foreign.R = &mtbCountryR{}
		}
		foreign.R.CountryDTBTaxRules = append(foreign.R.CountryDTBTaxRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CountryID, foreign.ID) {
				local.R.Country = foreign
				if foreign.R == nil {
					foreign.R = &mtbCountryR{}
				}
				foreign.R.CountryDTBTaxRules = append(foreign.R.CountryDTBTaxRules, local)
				break
			}
		}
	}

	return nil
}

// SetRoundingType of the dtbTaxRule to the related item.
// Sets o.R.RoundingType to related.
// Adds o to related.R.RoundingTypeDTBTaxRules.
func (o *DTBTaxRule) SetRoundingType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MTBRoundingType) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_tax_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"rounding_type_id"}),
		strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RoundingTypeID, related.ID)
	if o.R == nil {
		o.R = &dtbTaxRuleR{
			RoundingType: related,
		}
	} else {
		o.R.RoundingType = related
	}

	if related.R == nil {
		related.R = &mtbRoundingTypeR{
			RoundingTypeDTBTaxRules: DTBTaxRuleSlice{o},
		}
	} else {
		related.R.RoundingTypeDTBTaxRules = append(related.R.RoundingTypeDTBTaxRules, o)
	}

	return nil
}

// RemoveRoundingType relationship.
// Sets o.R.RoundingType to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBTaxRule) RemoveRoundingType(ctx context.Context, exec boil.ContextExecutor, related *MTBRoundingType) error {
	var err error

	queries.SetScanner(&o.RoundingTypeID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("rounding_type_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.RoundingType = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RoundingTypeDTBTaxRules {
		if queries.Equal(o.RoundingTypeID, ri.RoundingTypeID) {
			continue
		}

		ln := len(related.R.RoundingTypeDTBTaxRules)
		if ln > 1 && i < ln-1 {
			related.R.RoundingTypeDTBTaxRules[i] = related.R.RoundingTypeDTBTaxRules[ln-1]
		}
		related.R.RoundingTypeDTBTaxRules = related.R.RoundingTypeDTBTaxRules[:ln-1]
		break
	}
	return nil
}

// SetProduct of the dtbTaxRule to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.ProductDTBTaxRules.
func (o *DTBTaxRule) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DTBProduct) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_tax_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"product_id"}),
		strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ProductID, related.ID)
	if o.R == nil {
		o.R = &dtbTaxRuleR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &dtbProductR{
			ProductDTBTaxRules: DTBTaxRuleSlice{o},
		}
	} else {
		related.R.ProductDTBTaxRules = append(related.R.ProductDTBTaxRules, o)
	}

	return nil
}

// RemoveProduct relationship.
// Sets o.R.Product to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBTaxRule) RemoveProduct(ctx context.Context, exec boil.ContextExecutor, related *DTBProduct) error {
	var err error

	queries.SetScanner(&o.ProductID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("product_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Product = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ProductDTBTaxRules {
		if queries.Equal(o.ProductID, ri.ProductID) {
			continue
		}

		ln := len(related.R.ProductDTBTaxRules)
		if ln > 1 && i < ln-1 {
			related.R.ProductDTBTaxRules[i] = related.R.ProductDTBTaxRules[ln-1]
		}
		related.R.ProductDTBTaxRules = related.R.ProductDTBTaxRules[:ln-1]
		break
	}
	return nil
}

// SetPref of the dtbTaxRule to the related item.
// Sets o.R.Pref to related.
// Adds o to related.R.PrefDTBTaxRules.
func (o *DTBTaxRule) SetPref(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MTBPref) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_tax_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pref_id"}),
		strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.PrefID, related.ID)
	if o.R == nil {
		o.R = &dtbTaxRuleR{
			Pref: related,
		}
	} else {
		o.R.Pref = related
	}

	if related.R == nil {
		related.R = &mtbPrefR{
			PrefDTBTaxRules: DTBTaxRuleSlice{o},
		}
	} else {
		related.R.PrefDTBTaxRules = append(related.R.PrefDTBTaxRules, o)
	}

	return nil
}

// RemovePref relationship.
// Sets o.R.Pref to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBTaxRule) RemovePref(ctx context.Context, exec boil.ContextExecutor, related *MTBPref) error {
	var err error

	queries.SetScanner(&o.PrefID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("pref_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Pref = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.PrefDTBTaxRules {
		if queries.Equal(o.PrefID, ri.PrefID) {
			continue
		}

		ln := len(related.R.PrefDTBTaxRules)
		if ln > 1 && i < ln-1 {
			related.R.PrefDTBTaxRules[i] = related.R.PrefDTBTaxRules[ln-1]
		}
		related.R.PrefDTBTaxRules = related.R.PrefDTBTaxRules[:ln-1]
		break
	}
	return nil
}

// SetCountry of the dtbTaxRule to the related item.
// Sets o.R.Country to related.
// Adds o to related.R.CountryDTBTaxRules.
func (o *DTBTaxRule) SetCountry(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MTBCountry) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_tax_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"country_id"}),
		strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CountryID, related.ID)
	if o.R == nil {
		o.R = &dtbTaxRuleR{
			Country: related,
		}
	} else {
		o.R.Country = related
	}

	if related.R == nil {
		related.R = &mtbCountryR{
			CountryDTBTaxRules: DTBTaxRuleSlice{o},
		}
	} else {
		related.R.CountryDTBTaxRules = append(related.R.CountryDTBTaxRules, o)
	}

	return nil
}

// RemoveCountry relationship.
// Sets o.R.Country to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBTaxRule) RemoveCountry(ctx context.Context, exec boil.ContextExecutor, related *MTBCountry) error {
	var err error

	queries.SetScanner(&o.CountryID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("country_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Country = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CountryDTBTaxRules {
		if queries.Equal(o.CountryID, ri.CountryID) {
			continue
		}

		ln := len(related.R.CountryDTBTaxRules)
		if ln > 1 && i < ln-1 {
			related.R.CountryDTBTaxRules[i] = related.R.CountryDTBTaxRules[ln-1]
		}
		related.R.CountryDTBTaxRules = related.R.CountryDTBTaxRules[:ln-1]
		break
	}
	return nil
}

// DTBTaxRules retrieves all the records using an executor.
func DTBTaxRules(mods ...qm.QueryMod) dtbTaxRuleQuery {
	mods = append(mods, qm.From("`dtb_tax_rule`"))
	return dtbTaxRuleQuery{NewQuery(mods...)}
}

// FindDTBTaxRule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDTBTaxRule(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*DTBTaxRule, error) {
	dtbTaxRuleObj := &DTBTaxRule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `dtb_tax_rule` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dtbTaxRuleObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from dtb_tax_rule")
	}

	return dtbTaxRuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DTBTaxRule) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_tax_rule provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbTaxRuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dtbTaxRuleInsertCacheMut.RLock()
	cache, cached := dtbTaxRuleInsertCache[key]
	dtbTaxRuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dtbTaxRuleColumns,
			dtbTaxRuleColumnsWithDefault,
			dtbTaxRuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `dtb_tax_rule` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `dtb_tax_rule` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `dtb_tax_rule` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into dtb_tax_rule")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbTaxRuleMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_tax_rule")
	}

CacheNoHooks:
	if !cached {
		dtbTaxRuleInsertCacheMut.Lock()
		dtbTaxRuleInsertCache[key] = cache
		dtbTaxRuleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DTBTaxRule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DTBTaxRule) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dtbTaxRuleUpdateCacheMut.RLock()
	cache, cached := dtbTaxRuleUpdateCache[key]
	dtbTaxRuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dtbTaxRuleColumns,
			dtbTaxRulePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update dtb_tax_rule, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `dtb_tax_rule` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dtbTaxRulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, append(wl, dtbTaxRulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update dtb_tax_rule row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for dtb_tax_rule")
	}

	if !cached {
		dtbTaxRuleUpdateCacheMut.Lock()
		dtbTaxRuleUpdateCache[key] = cache
		dtbTaxRuleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q dtbTaxRuleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for dtb_tax_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for dtb_tax_rule")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DTBTaxRuleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbTaxRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `dtb_tax_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbTaxRulePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in dtbTaxRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all dtbTaxRule")
	}
	return rowsAff, nil
}

var mySQLDTBTaxRuleUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DTBTaxRule) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_tax_rule provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbTaxRuleColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDTBTaxRuleUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dtbTaxRuleUpsertCacheMut.RLock()
	cache, cached := dtbTaxRuleUpsertCache[key]
	dtbTaxRuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dtbTaxRuleColumns,
			dtbTaxRuleColumnsWithDefault,
			dtbTaxRuleColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			dtbTaxRuleColumns,
			dtbTaxRulePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("model: unable to upsert dtb_tax_rule, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "dtb_tax_rule", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `dtb_tax_rule` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to upsert for dtb_tax_rule")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbTaxRuleMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dtbTaxRuleType, dtbTaxRuleMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "model: unable to retrieve unique values for dtb_tax_rule")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_tax_rule")
	}

CacheNoHooks:
	if !cached {
		dtbTaxRuleUpsertCacheMut.Lock()
		dtbTaxRuleUpsertCache[key] = cache
		dtbTaxRuleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DTBTaxRule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DTBTaxRule) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBTaxRule provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dtbTaxRulePrimaryKeyMapping)
	sql := "DELETE FROM `dtb_tax_rule` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from dtb_tax_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for dtb_tax_rule")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q dtbTaxRuleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no dtbTaxRuleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtb_tax_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_tax_rule")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DTBTaxRuleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBTaxRule slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(dtbTaxRuleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbTaxRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `dtb_tax_rule` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbTaxRulePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtbTaxRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_tax_rule")
	}

	if len(dtbTaxRuleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DTBTaxRule) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDTBTaxRule(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DTBTaxRuleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DTBTaxRuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbTaxRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `dtb_tax_rule`.* FROM `dtb_tax_rule` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbTaxRulePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in DTBTaxRuleSlice")
	}

	*o = slice

	return nil
}

// DTBTaxRuleExists checks if the DTBTaxRule row exists.
func DTBTaxRuleExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `dtb_tax_rule` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if dtb_tax_rule exists")
	}

	return exists, nil
}
