// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// DTBDeliveryFee is an object representing the database table.
type DTBDeliveryFee struct {
	ID                uint          `boil:"id" json:"id" toml:"id" yaml:"id"`
	DeliveryID        null.Uint     `boil:"delivery_id" json:"delivery_id,omitempty" toml:"delivery_id" yaml:"delivery_id,omitempty"`
	PrefID            null.Uint16   `boil:"pref_id" json:"pref_id,omitempty" toml:"pref_id" yaml:"pref_id,omitempty"`
	Fee               types.Decimal `boil:"fee" json:"fee" toml:"fee" yaml:"fee"`
	DiscriminatorType string        `boil:"discriminator_type" json:"discriminator_type" toml:"discriminator_type" yaml:"discriminator_type"`

	R *dtbDeliveryFeeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dtbDeliveryFeeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DTBDeliveryFeeColumns = struct {
	ID                string
	DeliveryID        string
	PrefID            string
	Fee               string
	DiscriminatorType string
}{
	ID:                "id",
	DeliveryID:        "delivery_id",
	PrefID:            "pref_id",
	Fee:               "fee",
	DiscriminatorType: "discriminator_type",
}

// Generated where

var DTBDeliveryFeeWhere = struct {
	ID                whereHelperuint
	DeliveryID        whereHelpernull_Uint
	PrefID            whereHelpernull_Uint16
	Fee               whereHelpertypes_Decimal
	DiscriminatorType whereHelperstring
}{
	ID:                whereHelperuint{field: `id`},
	DeliveryID:        whereHelpernull_Uint{field: `delivery_id`},
	PrefID:            whereHelpernull_Uint16{field: `pref_id`},
	Fee:               whereHelpertypes_Decimal{field: `fee`},
	DiscriminatorType: whereHelperstring{field: `discriminator_type`},
}

// DTBDeliveryFeeRels is where relationship names are stored.
var DTBDeliveryFeeRels = struct {
	Delivery string
	Pref     string
}{
	Delivery: "Delivery",
	Pref:     "Pref",
}

// dtbDeliveryFeeR is where relationships are stored.
type dtbDeliveryFeeR struct {
	Delivery *DTBDelivery
	Pref     *MTBPref
}

// NewStruct creates a new relationship struct
func (*dtbDeliveryFeeR) NewStruct() *dtbDeliveryFeeR {
	return &dtbDeliveryFeeR{}
}

// dtbDeliveryFeeL is where Load methods for each relationship are stored.
type dtbDeliveryFeeL struct{}

var (
	dtbDeliveryFeeColumns               = []string{"id", "delivery_id", "pref_id", "fee", "discriminator_type"}
	dtbDeliveryFeeColumnsWithoutDefault = []string{"delivery_id", "pref_id", "fee", "discriminator_type"}
	dtbDeliveryFeeColumnsWithDefault    = []string{"id"}
	dtbDeliveryFeePrimaryKeyColumns     = []string{"id"}
)

type (
	// DTBDeliveryFeeSlice is an alias for a slice of pointers to DTBDeliveryFee.
	// This should generally be used opposed to []DTBDeliveryFee.
	DTBDeliveryFeeSlice []*DTBDeliveryFee
	// DTBDeliveryFeeHook is the signature for custom DTBDeliveryFee hook methods
	DTBDeliveryFeeHook func(context.Context, boil.ContextExecutor, *DTBDeliveryFee) error

	dtbDeliveryFeeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dtbDeliveryFeeType                 = reflect.TypeOf(&DTBDeliveryFee{})
	dtbDeliveryFeeMapping              = queries.MakeStructMapping(dtbDeliveryFeeType)
	dtbDeliveryFeePrimaryKeyMapping, _ = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, dtbDeliveryFeePrimaryKeyColumns)
	dtbDeliveryFeeInsertCacheMut       sync.RWMutex
	dtbDeliveryFeeInsertCache          = make(map[string]insertCache)
	dtbDeliveryFeeUpdateCacheMut       sync.RWMutex
	dtbDeliveryFeeUpdateCache          = make(map[string]updateCache)
	dtbDeliveryFeeUpsertCacheMut       sync.RWMutex
	dtbDeliveryFeeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dtbDeliveryFeeBeforeInsertHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeBeforeUpdateHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeBeforeDeleteHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeBeforeUpsertHooks []DTBDeliveryFeeHook

var dtbDeliveryFeeAfterInsertHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeAfterSelectHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeAfterUpdateHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeAfterDeleteHooks []DTBDeliveryFeeHook
var dtbDeliveryFeeAfterUpsertHooks []DTBDeliveryFeeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DTBDeliveryFee) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DTBDeliveryFee) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DTBDeliveryFee) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DTBDeliveryFee) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DTBDeliveryFee) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DTBDeliveryFee) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DTBDeliveryFee) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DTBDeliveryFee) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DTBDeliveryFee) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbDeliveryFeeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDTBDeliveryFeeHook registers your hook function for all future operations.
func AddDTBDeliveryFeeHook(hookPoint boil.HookPoint, dtbDeliveryFeeHook DTBDeliveryFeeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		dtbDeliveryFeeBeforeInsertHooks = append(dtbDeliveryFeeBeforeInsertHooks, dtbDeliveryFeeHook)
	case boil.BeforeUpdateHook:
		dtbDeliveryFeeBeforeUpdateHooks = append(dtbDeliveryFeeBeforeUpdateHooks, dtbDeliveryFeeHook)
	case boil.BeforeDeleteHook:
		dtbDeliveryFeeBeforeDeleteHooks = append(dtbDeliveryFeeBeforeDeleteHooks, dtbDeliveryFeeHook)
	case boil.BeforeUpsertHook:
		dtbDeliveryFeeBeforeUpsertHooks = append(dtbDeliveryFeeBeforeUpsertHooks, dtbDeliveryFeeHook)
	case boil.AfterInsertHook:
		dtbDeliveryFeeAfterInsertHooks = append(dtbDeliveryFeeAfterInsertHooks, dtbDeliveryFeeHook)
	case boil.AfterSelectHook:
		dtbDeliveryFeeAfterSelectHooks = append(dtbDeliveryFeeAfterSelectHooks, dtbDeliveryFeeHook)
	case boil.AfterUpdateHook:
		dtbDeliveryFeeAfterUpdateHooks = append(dtbDeliveryFeeAfterUpdateHooks, dtbDeliveryFeeHook)
	case boil.AfterDeleteHook:
		dtbDeliveryFeeAfterDeleteHooks = append(dtbDeliveryFeeAfterDeleteHooks, dtbDeliveryFeeHook)
	case boil.AfterUpsertHook:
		dtbDeliveryFeeAfterUpsertHooks = append(dtbDeliveryFeeAfterUpsertHooks, dtbDeliveryFeeHook)
	}
}

// One returns a single dtbDeliveryFee record from the query.
func (q dtbDeliveryFeeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DTBDeliveryFee, error) {
	o := &DTBDeliveryFee{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for dtb_delivery_fee")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DTBDeliveryFee records from the query.
func (q dtbDeliveryFeeQuery) All(ctx context.Context, exec boil.ContextExecutor) (DTBDeliveryFeeSlice, error) {
	var o []*DTBDeliveryFee

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to DTBDeliveryFee slice")
	}

	if len(dtbDeliveryFeeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DTBDeliveryFee records in the query.
func (q dtbDeliveryFeeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count dtb_delivery_fee rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dtbDeliveryFeeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if dtb_delivery_fee exists")
	}

	return count > 0, nil
}

// Delivery pointed to by the foreign key.
func (o *DTBDeliveryFee) Delivery(mods ...qm.QueryMod) dtbDeliveryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.DeliveryID),
	}

	queryMods = append(queryMods, mods...)

	query := DTBDeliveries(queryMods...)
	queries.SetFrom(query.Query, "`dtb_delivery`")

	return query
}

// Pref pointed to by the foreign key.
func (o *DTBDeliveryFee) Pref(mods ...qm.QueryMod) mtbPrefQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.PrefID),
	}

	queryMods = append(queryMods, mods...)

	query := MTBPrefs(queryMods...)
	queries.SetFrom(query.Query, "`mtb_pref`")

	return query
}

// LoadDelivery allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbDeliveryFeeL) LoadDelivery(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBDeliveryFee interface{}, mods queries.Applicator) error {
	var slice []*DTBDeliveryFee
	var object *DTBDeliveryFee

	if singular {
		object = maybeDTBDeliveryFee.(*DTBDeliveryFee)
	} else {
		slice = *maybeDTBDeliveryFee.(*[]*DTBDeliveryFee)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbDeliveryFeeR{}
		}
		if !queries.IsNil(object.DeliveryID) {
			args = append(args, object.DeliveryID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbDeliveryFeeR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.DeliveryID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.DeliveryID) {
				args = append(args, obj.DeliveryID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dtb_delivery`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DTBDelivery")
	}

	var resultSlice []*DTBDelivery
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DTBDelivery")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for dtb_delivery")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for dtb_delivery")
	}

	if len(dtbDeliveryFeeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Delivery = foreign
		if foreign.R == nil {
			foreign.R = &dtbDeliveryR{}
		}
		foreign.R.DeliveryDTBDeliveryFees = append(foreign.R.DeliveryDTBDeliveryFees, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.DeliveryID, foreign.ID) {
				local.R.Delivery = foreign
				if foreign.R == nil {
					foreign.R = &dtbDeliveryR{}
				}
				foreign.R.DeliveryDTBDeliveryFees = append(foreign.R.DeliveryDTBDeliveryFees, local)
				break
			}
		}
	}

	return nil
}

// LoadPref allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbDeliveryFeeL) LoadPref(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBDeliveryFee interface{}, mods queries.Applicator) error {
	var slice []*DTBDeliveryFee
	var object *DTBDeliveryFee

	if singular {
		object = maybeDTBDeliveryFee.(*DTBDeliveryFee)
	} else {
		slice = *maybeDTBDeliveryFee.(*[]*DTBDeliveryFee)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbDeliveryFeeR{}
		}
		if !queries.IsNil(object.PrefID) {
			args = append(args, object.PrefID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbDeliveryFeeR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.PrefID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.PrefID) {
				args = append(args, obj.PrefID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`mtb_pref`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MTBPref")
	}

	var resultSlice []*MTBPref
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MTBPref")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mtb_pref")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mtb_pref")
	}

	if len(dtbDeliveryFeeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Pref = foreign
		if foreign.R == nil {
			foreign.R = &mtbPrefR{}
		}
		foreign.R.PrefDTBDeliveryFees = append(foreign.R.PrefDTBDeliveryFees, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.PrefID, foreign.ID) {
				local.R.Pref = foreign
				if foreign.R == nil {
					foreign.R = &mtbPrefR{}
				}
				foreign.R.PrefDTBDeliveryFees = append(foreign.R.PrefDTBDeliveryFees, local)
				break
			}
		}
	}

	return nil
}

// SetDelivery of the dtbDeliveryFee to the related item.
// Sets o.R.Delivery to related.
// Adds o to related.R.DeliveryDTBDeliveryFees.
func (o *DTBDeliveryFee) SetDelivery(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DTBDelivery) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_delivery_fee` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"delivery_id"}),
		strmangle.WhereClause("`", "`", 0, dtbDeliveryFeePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.DeliveryID, related.ID)
	if o.R == nil {
		o.R = &dtbDeliveryFeeR{
			Delivery: related,
		}
	} else {
		o.R.Delivery = related
	}

	if related.R == nil {
		related.R = &dtbDeliveryR{
			DeliveryDTBDeliveryFees: DTBDeliveryFeeSlice{o},
		}
	} else {
		related.R.DeliveryDTBDeliveryFees = append(related.R.DeliveryDTBDeliveryFees, o)
	}

	return nil
}

// RemoveDelivery relationship.
// Sets o.R.Delivery to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBDeliveryFee) RemoveDelivery(ctx context.Context, exec boil.ContextExecutor, related *DTBDelivery) error {
	var err error

	queries.SetScanner(&o.DeliveryID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("delivery_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Delivery = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.DeliveryDTBDeliveryFees {
		if queries.Equal(o.DeliveryID, ri.DeliveryID) {
			continue
		}

		ln := len(related.R.DeliveryDTBDeliveryFees)
		if ln > 1 && i < ln-1 {
			related.R.DeliveryDTBDeliveryFees[i] = related.R.DeliveryDTBDeliveryFees[ln-1]
		}
		related.R.DeliveryDTBDeliveryFees = related.R.DeliveryDTBDeliveryFees[:ln-1]
		break
	}
	return nil
}

// SetPref of the dtbDeliveryFee to the related item.
// Sets o.R.Pref to related.
// Adds o to related.R.PrefDTBDeliveryFees.
func (o *DTBDeliveryFee) SetPref(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MTBPref) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_delivery_fee` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"pref_id"}),
		strmangle.WhereClause("`", "`", 0, dtbDeliveryFeePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.PrefID, related.ID)
	if o.R == nil {
		o.R = &dtbDeliveryFeeR{
			Pref: related,
		}
	} else {
		o.R.Pref = related
	}

	if related.R == nil {
		related.R = &mtbPrefR{
			PrefDTBDeliveryFees: DTBDeliveryFeeSlice{o},
		}
	} else {
		related.R.PrefDTBDeliveryFees = append(related.R.PrefDTBDeliveryFees, o)
	}

	return nil
}

// RemovePref relationship.
// Sets o.R.Pref to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBDeliveryFee) RemovePref(ctx context.Context, exec boil.ContextExecutor, related *MTBPref) error {
	var err error

	queries.SetScanner(&o.PrefID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("pref_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Pref = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.PrefDTBDeliveryFees {
		if queries.Equal(o.PrefID, ri.PrefID) {
			continue
		}

		ln := len(related.R.PrefDTBDeliveryFees)
		if ln > 1 && i < ln-1 {
			related.R.PrefDTBDeliveryFees[i] = related.R.PrefDTBDeliveryFees[ln-1]
		}
		related.R.PrefDTBDeliveryFees = related.R.PrefDTBDeliveryFees[:ln-1]
		break
	}
	return nil
}

// DTBDeliveryFees retrieves all the records using an executor.
func DTBDeliveryFees(mods ...qm.QueryMod) dtbDeliveryFeeQuery {
	mods = append(mods, qm.From("`dtb_delivery_fee`"))
	return dtbDeliveryFeeQuery{NewQuery(mods...)}
}

// FindDTBDeliveryFee retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDTBDeliveryFee(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*DTBDeliveryFee, error) {
	dtbDeliveryFeeObj := &DTBDeliveryFee{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `dtb_delivery_fee` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dtbDeliveryFeeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from dtb_delivery_fee")
	}

	return dtbDeliveryFeeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DTBDeliveryFee) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_delivery_fee provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbDeliveryFeeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dtbDeliveryFeeInsertCacheMut.RLock()
	cache, cached := dtbDeliveryFeeInsertCache[key]
	dtbDeliveryFeeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dtbDeliveryFeeColumns,
			dtbDeliveryFeeColumnsWithDefault,
			dtbDeliveryFeeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `dtb_delivery_fee` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `dtb_delivery_fee` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `dtb_delivery_fee` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dtbDeliveryFeePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into dtb_delivery_fee")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbDeliveryFeeMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_delivery_fee")
	}

CacheNoHooks:
	if !cached {
		dtbDeliveryFeeInsertCacheMut.Lock()
		dtbDeliveryFeeInsertCache[key] = cache
		dtbDeliveryFeeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DTBDeliveryFee.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DTBDeliveryFee) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dtbDeliveryFeeUpdateCacheMut.RLock()
	cache, cached := dtbDeliveryFeeUpdateCache[key]
	dtbDeliveryFeeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dtbDeliveryFeeColumns,
			dtbDeliveryFeePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update dtb_delivery_fee, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `dtb_delivery_fee` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dtbDeliveryFeePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, append(wl, dtbDeliveryFeePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update dtb_delivery_fee row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for dtb_delivery_fee")
	}

	if !cached {
		dtbDeliveryFeeUpdateCacheMut.Lock()
		dtbDeliveryFeeUpdateCache[key] = cache
		dtbDeliveryFeeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q dtbDeliveryFeeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for dtb_delivery_fee")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for dtb_delivery_fee")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DTBDeliveryFeeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbDeliveryFeePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `dtb_delivery_fee` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbDeliveryFeePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in dtbDeliveryFee slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all dtbDeliveryFee")
	}
	return rowsAff, nil
}

var mySQLDTBDeliveryFeeUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DTBDeliveryFee) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_delivery_fee provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbDeliveryFeeColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDTBDeliveryFeeUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dtbDeliveryFeeUpsertCacheMut.RLock()
	cache, cached := dtbDeliveryFeeUpsertCache[key]
	dtbDeliveryFeeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dtbDeliveryFeeColumns,
			dtbDeliveryFeeColumnsWithDefault,
			dtbDeliveryFeeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			dtbDeliveryFeeColumns,
			dtbDeliveryFeePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("model: unable to upsert dtb_delivery_fee, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "dtb_delivery_fee", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `dtb_delivery_fee` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to upsert for dtb_delivery_fee")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbDeliveryFeeMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dtbDeliveryFeeType, dtbDeliveryFeeMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "model: unable to retrieve unique values for dtb_delivery_fee")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_delivery_fee")
	}

CacheNoHooks:
	if !cached {
		dtbDeliveryFeeUpsertCacheMut.Lock()
		dtbDeliveryFeeUpsertCache[key] = cache
		dtbDeliveryFeeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DTBDeliveryFee record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DTBDeliveryFee) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBDeliveryFee provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dtbDeliveryFeePrimaryKeyMapping)
	sql := "DELETE FROM `dtb_delivery_fee` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from dtb_delivery_fee")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for dtb_delivery_fee")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q dtbDeliveryFeeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no dtbDeliveryFeeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtb_delivery_fee")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_delivery_fee")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DTBDeliveryFeeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBDeliveryFee slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(dtbDeliveryFeeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbDeliveryFeePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `dtb_delivery_fee` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbDeliveryFeePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtbDeliveryFee slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_delivery_fee")
	}

	if len(dtbDeliveryFeeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DTBDeliveryFee) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDTBDeliveryFee(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DTBDeliveryFeeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DTBDeliveryFeeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbDeliveryFeePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `dtb_delivery_fee`.* FROM `dtb_delivery_fee` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbDeliveryFeePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in DTBDeliveryFeeSlice")
	}

	*o = slice

	return nil
}

// DTBDeliveryFeeExists checks if the DTBDeliveryFee row exists.
func DTBDeliveryFeeExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `dtb_delivery_fee` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if dtb_delivery_fee exists")
	}

	return exists, nil
}
