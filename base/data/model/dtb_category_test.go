// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDTBCategories(t *testing.T) {
	t.Parallel()

	query := DTBCategories()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDTBCategoriesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDTBCategoriesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DTBCategories().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDTBCategoriesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DTBCategorySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDTBCategoriesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DTBCategoryExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DTBCategory exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DTBCategoryExists to return true, but got false.")
	}
}

func testDTBCategoriesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	dtbCategoryFound, err := FindDTBCategory(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if dtbCategoryFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDTBCategoriesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DTBCategories().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDTBCategoriesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DTBCategories().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDTBCategoriesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	dtbCategoryOne := &DTBCategory{}
	dtbCategoryTwo := &DTBCategory{}
	if err = randomize.Struct(seed, dtbCategoryOne, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}
	if err = randomize.Struct(seed, dtbCategoryTwo, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dtbCategoryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dtbCategoryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DTBCategories().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDTBCategoriesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	dtbCategoryOne := &DTBCategory{}
	dtbCategoryTwo := &DTBCategory{}
	if err = randomize.Struct(seed, dtbCategoryOne, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}
	if err = randomize.Struct(seed, dtbCategoryTwo, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dtbCategoryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dtbCategoryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func dtbCategoryBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func dtbCategoryAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DTBCategory) error {
	*o = DTBCategory{}
	return nil
}

func testDTBCategoriesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &DTBCategory{}
	o := &DTBCategory{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DTBCategory object: %s", err)
	}

	AddDTBCategoryHook(boil.BeforeInsertHook, dtbCategoryBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	dtbCategoryBeforeInsertHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.AfterInsertHook, dtbCategoryAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	dtbCategoryAfterInsertHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.AfterSelectHook, dtbCategoryAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	dtbCategoryAfterSelectHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.BeforeUpdateHook, dtbCategoryBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	dtbCategoryBeforeUpdateHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.AfterUpdateHook, dtbCategoryAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	dtbCategoryAfterUpdateHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.BeforeDeleteHook, dtbCategoryBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	dtbCategoryBeforeDeleteHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.AfterDeleteHook, dtbCategoryAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	dtbCategoryAfterDeleteHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.BeforeUpsertHook, dtbCategoryBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	dtbCategoryBeforeUpsertHooks = []DTBCategoryHook{}

	AddDTBCategoryHook(boil.AfterUpsertHook, dtbCategoryAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	dtbCategoryAfterUpsertHooks = []DTBCategoryHook{}
}

func testDTBCategoriesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDTBCategoriesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(dtbCategoryColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDTBCategoryToManyParentCategoryDTBCategories(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.ParentCategoryID, a.ID)
	queries.Assign(&c.ParentCategoryID, a.ID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ParentCategoryDTBCategories().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.ParentCategoryID, b.ParentCategoryID) {
			bFound = true
		}
		if queries.Equal(v.ParentCategoryID, c.ParentCategoryID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DTBCategorySlice{&a}
	if err = a.L.LoadParentCategoryDTBCategories(ctx, tx, false, (*[]*DTBCategory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ParentCategoryDTBCategories); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ParentCategoryDTBCategories = nil
	if err = a.L.LoadParentCategoryDTBCategories(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ParentCategoryDTBCategories); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDTBCategoryToManyCategoryDTBProductCategories(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c DTBProductCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, dtbProductCategoryDBTypes, false, dtbProductCategoryColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dtbProductCategoryDBTypes, false, dtbProductCategoryColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.CategoryID = a.ID
	c.CategoryID = a.ID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.CategoryDTBProductCategories().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.CategoryID == b.CategoryID {
			bFound = true
		}
		if v.CategoryID == c.CategoryID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DTBCategorySlice{&a}
	if err = a.L.LoadCategoryDTBProductCategories(ctx, tx, false, (*[]*DTBCategory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.CategoryDTBProductCategories); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.CategoryDTBProductCategories = nil
	if err = a.L.LoadCategoryDTBProductCategories(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.CategoryDTBProductCategories); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDTBCategoryToManyAddOpParentCategoryDTBCategories(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c, d, e DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DTBCategory{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DTBCategory{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddParentCategoryDTBCategories(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.ParentCategoryID) {
			t.Error("foreign key was wrong value", a.ID, first.ParentCategoryID)
		}
		if !queries.Equal(a.ID, second.ParentCategoryID) {
			t.Error("foreign key was wrong value", a.ID, second.ParentCategoryID)
		}

		if first.R.ParentCategory != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.ParentCategory != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ParentCategoryDTBCategories[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ParentCategoryDTBCategories[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ParentCategoryDTBCategories().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDTBCategoryToManySetOpParentCategoryDTBCategories(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c, d, e DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DTBCategory{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetParentCategoryDTBCategories(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ParentCategoryDTBCategories().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetParentCategoryDTBCategories(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ParentCategoryDTBCategories().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ParentCategoryID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ParentCategoryID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.ParentCategoryID) {
		t.Error("foreign key was wrong value", a.ID, d.ParentCategoryID)
	}
	if !queries.Equal(a.ID, e.ParentCategoryID) {
		t.Error("foreign key was wrong value", a.ID, e.ParentCategoryID)
	}

	if b.R.ParentCategory != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.ParentCategory != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.ParentCategory != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.ParentCategory != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.ParentCategoryDTBCategories[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.ParentCategoryDTBCategories[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDTBCategoryToManyRemoveOpParentCategoryDTBCategories(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c, d, e DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DTBCategory{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddParentCategoryDTBCategories(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ParentCategoryDTBCategories().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveParentCategoryDTBCategories(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ParentCategoryDTBCategories().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ParentCategoryID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ParentCategoryID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.ParentCategory != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.ParentCategory != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.ParentCategory != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.ParentCategory != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.ParentCategoryDTBCategories) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.ParentCategoryDTBCategories[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.ParentCategoryDTBCategories[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDTBCategoryToManyAddOpCategoryDTBProductCategories(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c, d, e DTBProductCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DTBProductCategory{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, dtbProductCategoryDBTypes, false, strmangle.SetComplement(dtbProductCategoryPrimaryKeyColumns, dtbProductCategoryColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DTBProductCategory{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddCategoryDTBProductCategories(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.CategoryID {
			t.Error("foreign key was wrong value", a.ID, first.CategoryID)
		}
		if a.ID != second.CategoryID {
			t.Error("foreign key was wrong value", a.ID, second.CategoryID)
		}

		if first.R.Category != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Category != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.CategoryDTBProductCategories[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.CategoryDTBProductCategories[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.CategoryDTBProductCategories().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testDTBCategoryToOneDTBMemberUsingCreator(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DTBCategory
	var foreign DTBMember

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, dtbMemberDBTypes, false, dtbMemberColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBMember struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.CreatorID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Creator().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DTBCategorySlice{&local}
	if err = local.L.LoadCreator(ctx, tx, false, (*[]*DTBCategory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Creator == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Creator = nil
	if err = local.L.LoadCreator(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Creator == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDTBCategoryToOneDTBCategoryUsingParentCategory(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DTBCategory
	var foreign DTBCategory

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, dtbCategoryDBTypes, false, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ParentCategoryID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.ParentCategory().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DTBCategorySlice{&local}
	if err = local.L.LoadParentCategory(ctx, tx, false, (*[]*DTBCategory)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.ParentCategory == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.ParentCategory = nil
	if err = local.L.LoadParentCategory(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.ParentCategory == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDTBCategoryToOneSetOpDTBMemberUsingCreator(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c DTBMember

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dtbMemberDBTypes, false, strmangle.SetComplement(dtbMemberPrimaryKeyColumns, dtbMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dtbMemberDBTypes, false, strmangle.SetComplement(dtbMemberPrimaryKeyColumns, dtbMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*DTBMember{&b, &c} {
		err = a.SetCreator(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Creator != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.CreatorDTBCategories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.CreatorID, x.ID) {
			t.Error("foreign key was wrong value", a.CreatorID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.CreatorID))
		reflect.Indirect(reflect.ValueOf(&a.CreatorID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.CreatorID, x.ID) {
			t.Error("foreign key was wrong value", a.CreatorID, x.ID)
		}
	}
}

func testDTBCategoryToOneRemoveOpDTBMemberUsingCreator(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b DTBMember

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dtbMemberDBTypes, false, strmangle.SetComplement(dtbMemberPrimaryKeyColumns, dtbMemberColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetCreator(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveCreator(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Creator().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Creator != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.CreatorID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.CreatorDTBCategories) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDTBCategoryToOneSetOpDTBCategoryUsingParentCategory(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b, c DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*DTBCategory{&b, &c} {
		err = a.SetParentCategory(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.ParentCategory != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ParentCategoryDTBCategories[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ParentCategoryID, x.ID) {
			t.Error("foreign key was wrong value", a.ParentCategoryID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ParentCategoryID))
		reflect.Indirect(reflect.ValueOf(&a.ParentCategoryID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ParentCategoryID, x.ID) {
			t.Error("foreign key was wrong value", a.ParentCategoryID, x.ID)
		}
	}
}

func testDTBCategoryToOneRemoveOpDTBCategoryUsingParentCategory(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DTBCategory
	var b DTBCategory

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dtbCategoryDBTypes, false, strmangle.SetComplement(dtbCategoryPrimaryKeyColumns, dtbCategoryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetParentCategory(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveParentCategory(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.ParentCategory().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.ParentCategory != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ParentCategoryID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ParentCategoryDTBCategories) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDTBCategoriesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDTBCategoriesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DTBCategorySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDTBCategoriesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DTBCategories().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	dtbCategoryDBTypes = map[string]string{`ID`: `int`, `ParentCategoryID`: `int`, `CreatorID`: `int`, `CategoryName`: `varchar`, `Hierarchy`: `int`, `SortNo`: `int`, `CreateDate`: `datetime`, `UpdateDate`: `datetime`, `DiscriminatorType`: `varchar`}
	_                  = bytes.MinRead
)

func testDTBCategoriesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(dtbCategoryPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(dtbCategoryColumns) == len(dtbCategoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDTBCategoriesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(dtbCategoryColumns) == len(dtbCategoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DTBCategory{}
	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dtbCategoryDBTypes, true, dtbCategoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(dtbCategoryColumns, dtbCategoryPrimaryKeyColumns) {
		fields = dtbCategoryColumns
	} else {
		fields = strmangle.SetComplement(
			dtbCategoryColumns,
			dtbCategoryPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DTBCategorySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDTBCategoriesUpsert(t *testing.T) {
	t.Parallel()

	if len(dtbCategoryColumns) == len(dtbCategoryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLDTBCategoryUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DTBCategory{}
	if err = randomize.Struct(seed, &o, dtbCategoryDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DTBCategory: %s", err)
	}

	count, err := DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, dtbCategoryDBTypes, false, dtbCategoryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DTBCategory struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DTBCategory: %s", err)
	}

	count, err = DTBCategories().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
