// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// DTBLayout is an object representing the database table.
type DTBLayout struct {
	ID                uint        `boil:"id" json:"id" toml:"id" yaml:"id"`
	DeviceTypeID      null.Uint16 `boil:"device_type_id" json:"device_type_id,omitempty" toml:"device_type_id" yaml:"device_type_id,omitempty"`
	LayoutName        null.String `boil:"layout_name" json:"layout_name,omitempty" toml:"layout_name" yaml:"layout_name,omitempty"`
	CreateDate        time.Time   `boil:"create_date" json:"create_date" toml:"create_date" yaml:"create_date"`
	UpdateDate        time.Time   `boil:"update_date" json:"update_date" toml:"update_date" yaml:"update_date"`
	DiscriminatorType string      `boil:"discriminator_type" json:"discriminator_type" toml:"discriminator_type" yaml:"discriminator_type"`

	R *dtbLayoutR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dtbLayoutL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DTBLayoutColumns = struct {
	ID                string
	DeviceTypeID      string
	LayoutName        string
	CreateDate        string
	UpdateDate        string
	DiscriminatorType string
}{
	ID:                "id",
	DeviceTypeID:      "device_type_id",
	LayoutName:        "layout_name",
	CreateDate:        "create_date",
	UpdateDate:        "update_date",
	DiscriminatorType: "discriminator_type",
}

// Generated where

var DTBLayoutWhere = struct {
	ID                whereHelperuint
	DeviceTypeID      whereHelpernull_Uint16
	LayoutName        whereHelpernull_String
	CreateDate        whereHelpertime_Time
	UpdateDate        whereHelpertime_Time
	DiscriminatorType whereHelperstring
}{
	ID:                whereHelperuint{field: `id`},
	DeviceTypeID:      whereHelpernull_Uint16{field: `device_type_id`},
	LayoutName:        whereHelpernull_String{field: `layout_name`},
	CreateDate:        whereHelpertime_Time{field: `create_date`},
	UpdateDate:        whereHelpertime_Time{field: `update_date`},
	DiscriminatorType: whereHelperstring{field: `discriminator_type`},
}

// DTBLayoutRels is where relationship names are stored.
var DTBLayoutRels = struct {
	DeviceType              string
	LayoutDTBBlockPositions string
	LayoutDTBPageLayouts    string
}{
	DeviceType:              "DeviceType",
	LayoutDTBBlockPositions: "LayoutDTBBlockPositions",
	LayoutDTBPageLayouts:    "LayoutDTBPageLayouts",
}

// dtbLayoutR is where relationships are stored.
type dtbLayoutR struct {
	DeviceType              *MTBDeviceType
	LayoutDTBBlockPositions DTBBlockPositionSlice
	LayoutDTBPageLayouts    DTBPageLayoutSlice
}

// NewStruct creates a new relationship struct
func (*dtbLayoutR) NewStruct() *dtbLayoutR {
	return &dtbLayoutR{}
}

// dtbLayoutL is where Load methods for each relationship are stored.
type dtbLayoutL struct{}

var (
	dtbLayoutColumns               = []string{"id", "device_type_id", "layout_name", "create_date", "update_date", "discriminator_type"}
	dtbLayoutColumnsWithoutDefault = []string{"device_type_id", "layout_name", "create_date", "update_date", "discriminator_type"}
	dtbLayoutColumnsWithDefault    = []string{"id"}
	dtbLayoutPrimaryKeyColumns     = []string{"id"}
)

type (
	// DTBLayoutSlice is an alias for a slice of pointers to DTBLayout.
	// This should generally be used opposed to []DTBLayout.
	DTBLayoutSlice []*DTBLayout
	// DTBLayoutHook is the signature for custom DTBLayout hook methods
	DTBLayoutHook func(context.Context, boil.ContextExecutor, *DTBLayout) error

	dtbLayoutQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dtbLayoutType                 = reflect.TypeOf(&DTBLayout{})
	dtbLayoutMapping              = queries.MakeStructMapping(dtbLayoutType)
	dtbLayoutPrimaryKeyMapping, _ = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, dtbLayoutPrimaryKeyColumns)
	dtbLayoutInsertCacheMut       sync.RWMutex
	dtbLayoutInsertCache          = make(map[string]insertCache)
	dtbLayoutUpdateCacheMut       sync.RWMutex
	dtbLayoutUpdateCache          = make(map[string]updateCache)
	dtbLayoutUpsertCacheMut       sync.RWMutex
	dtbLayoutUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dtbLayoutBeforeInsertHooks []DTBLayoutHook
var dtbLayoutBeforeUpdateHooks []DTBLayoutHook
var dtbLayoutBeforeDeleteHooks []DTBLayoutHook
var dtbLayoutBeforeUpsertHooks []DTBLayoutHook

var dtbLayoutAfterInsertHooks []DTBLayoutHook
var dtbLayoutAfterSelectHooks []DTBLayoutHook
var dtbLayoutAfterUpdateHooks []DTBLayoutHook
var dtbLayoutAfterDeleteHooks []DTBLayoutHook
var dtbLayoutAfterUpsertHooks []DTBLayoutHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DTBLayout) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DTBLayout) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DTBLayout) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DTBLayout) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DTBLayout) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DTBLayout) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DTBLayout) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DTBLayout) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DTBLayout) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dtbLayoutAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDTBLayoutHook registers your hook function for all future operations.
func AddDTBLayoutHook(hookPoint boil.HookPoint, dtbLayoutHook DTBLayoutHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		dtbLayoutBeforeInsertHooks = append(dtbLayoutBeforeInsertHooks, dtbLayoutHook)
	case boil.BeforeUpdateHook:
		dtbLayoutBeforeUpdateHooks = append(dtbLayoutBeforeUpdateHooks, dtbLayoutHook)
	case boil.BeforeDeleteHook:
		dtbLayoutBeforeDeleteHooks = append(dtbLayoutBeforeDeleteHooks, dtbLayoutHook)
	case boil.BeforeUpsertHook:
		dtbLayoutBeforeUpsertHooks = append(dtbLayoutBeforeUpsertHooks, dtbLayoutHook)
	case boil.AfterInsertHook:
		dtbLayoutAfterInsertHooks = append(dtbLayoutAfterInsertHooks, dtbLayoutHook)
	case boil.AfterSelectHook:
		dtbLayoutAfterSelectHooks = append(dtbLayoutAfterSelectHooks, dtbLayoutHook)
	case boil.AfterUpdateHook:
		dtbLayoutAfterUpdateHooks = append(dtbLayoutAfterUpdateHooks, dtbLayoutHook)
	case boil.AfterDeleteHook:
		dtbLayoutAfterDeleteHooks = append(dtbLayoutAfterDeleteHooks, dtbLayoutHook)
	case boil.AfterUpsertHook:
		dtbLayoutAfterUpsertHooks = append(dtbLayoutAfterUpsertHooks, dtbLayoutHook)
	}
}

// One returns a single dtbLayout record from the query.
func (q dtbLayoutQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DTBLayout, error) {
	o := &DTBLayout{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for dtb_layout")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all DTBLayout records from the query.
func (q dtbLayoutQuery) All(ctx context.Context, exec boil.ContextExecutor) (DTBLayoutSlice, error) {
	var o []*DTBLayout

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to DTBLayout slice")
	}

	if len(dtbLayoutAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all DTBLayout records in the query.
func (q dtbLayoutQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count dtb_layout rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dtbLayoutQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if dtb_layout exists")
	}

	return count > 0, nil
}

// DeviceType pointed to by the foreign key.
func (o *DTBLayout) DeviceType(mods ...qm.QueryMod) mtbDeviceTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.DeviceTypeID),
	}

	queryMods = append(queryMods, mods...)

	query := MTBDeviceTypes(queryMods...)
	queries.SetFrom(query.Query, "`mtb_device_type`")

	return query
}

// LayoutDTBBlockPositions retrieves all the dtb_block_position's DTBBlockPositions with an executor via layout_id column.
func (o *DTBLayout) LayoutDTBBlockPositions(mods ...qm.QueryMod) dtbBlockPositionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`dtb_block_position`.`layout_id`=?", o.ID),
	)

	query := DTBBlockPositions(queryMods...)
	queries.SetFrom(query.Query, "`dtb_block_position`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`dtb_block_position`.*"})
	}

	return query
}

// LayoutDTBPageLayouts retrieves all the dtb_page_layout's DTBPageLayouts with an executor via layout_id column.
func (o *DTBLayout) LayoutDTBPageLayouts(mods ...qm.QueryMod) dtbPageLayoutQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`dtb_page_layout`.`layout_id`=?", o.ID),
	)

	query := DTBPageLayouts(queryMods...)
	queries.SetFrom(query.Query, "`dtb_page_layout`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`dtb_page_layout`.*"})
	}

	return query
}

// LoadDeviceType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dtbLayoutL) LoadDeviceType(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBLayout interface{}, mods queries.Applicator) error {
	var slice []*DTBLayout
	var object *DTBLayout

	if singular {
		object = maybeDTBLayout.(*DTBLayout)
	} else {
		slice = *maybeDTBLayout.(*[]*DTBLayout)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbLayoutR{}
		}
		if !queries.IsNil(object.DeviceTypeID) {
			args = append(args, object.DeviceTypeID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbLayoutR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.DeviceTypeID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.DeviceTypeID) {
				args = append(args, obj.DeviceTypeID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`mtb_device_type`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MTBDeviceType")
	}

	var resultSlice []*MTBDeviceType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MTBDeviceType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mtb_device_type")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mtb_device_type")
	}

	if len(dtbLayoutAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DeviceType = foreign
		if foreign.R == nil {
			foreign.R = &mtbDeviceTypeR{}
		}
		foreign.R.DeviceTypeDTBLayouts = append(foreign.R.DeviceTypeDTBLayouts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.DeviceTypeID, foreign.ID) {
				local.R.DeviceType = foreign
				if foreign.R == nil {
					foreign.R = &mtbDeviceTypeR{}
				}
				foreign.R.DeviceTypeDTBLayouts = append(foreign.R.DeviceTypeDTBLayouts, local)
				break
			}
		}
	}

	return nil
}

// LoadLayoutDTBBlockPositions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (dtbLayoutL) LoadLayoutDTBBlockPositions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBLayout interface{}, mods queries.Applicator) error {
	var slice []*DTBLayout
	var object *DTBLayout

	if singular {
		object = maybeDTBLayout.(*DTBLayout)
	} else {
		slice = *maybeDTBLayout.(*[]*DTBLayout)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbLayoutR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbLayoutR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dtb_block_position`), qm.WhereIn(`layout_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load dtb_block_position")
	}

	var resultSlice []*DTBBlockPosition
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice dtb_block_position")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on dtb_block_position")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for dtb_block_position")
	}

	if len(dtbBlockPositionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.LayoutDTBBlockPositions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &dtbBlockPositionR{}
			}
			foreign.R.Layout = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.LayoutID {
				local.R.LayoutDTBBlockPositions = append(local.R.LayoutDTBBlockPositions, foreign)
				if foreign.R == nil {
					foreign.R = &dtbBlockPositionR{}
				}
				foreign.R.Layout = local
				break
			}
		}
	}

	return nil
}

// LoadLayoutDTBPageLayouts allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (dtbLayoutL) LoadLayoutDTBPageLayouts(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDTBLayout interface{}, mods queries.Applicator) error {
	var slice []*DTBLayout
	var object *DTBLayout

	if singular {
		object = maybeDTBLayout.(*DTBLayout)
	} else {
		slice = *maybeDTBLayout.(*[]*DTBLayout)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dtbLayoutR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dtbLayoutR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dtb_page_layout`), qm.WhereIn(`layout_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load dtb_page_layout")
	}

	var resultSlice []*DTBPageLayout
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice dtb_page_layout")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on dtb_page_layout")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for dtb_page_layout")
	}

	if len(dtbPageLayoutAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.LayoutDTBPageLayouts = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &dtbPageLayoutR{}
			}
			foreign.R.Layout = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.LayoutID {
				local.R.LayoutDTBPageLayouts = append(local.R.LayoutDTBPageLayouts, foreign)
				if foreign.R == nil {
					foreign.R = &dtbPageLayoutR{}
				}
				foreign.R.Layout = local
				break
			}
		}
	}

	return nil
}

// SetDeviceType of the dtbLayout to the related item.
// Sets o.R.DeviceType to related.
// Adds o to related.R.DeviceTypeDTBLayouts.
func (o *DTBLayout) SetDeviceType(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MTBDeviceType) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `dtb_layout` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"device_type_id"}),
		strmangle.WhereClause("`", "`", 0, dtbLayoutPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.DeviceTypeID, related.ID)
	if o.R == nil {
		o.R = &dtbLayoutR{
			DeviceType: related,
		}
	} else {
		o.R.DeviceType = related
	}

	if related.R == nil {
		related.R = &mtbDeviceTypeR{
			DeviceTypeDTBLayouts: DTBLayoutSlice{o},
		}
	} else {
		related.R.DeviceTypeDTBLayouts = append(related.R.DeviceTypeDTBLayouts, o)
	}

	return nil
}

// RemoveDeviceType relationship.
// Sets o.R.DeviceType to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DTBLayout) RemoveDeviceType(ctx context.Context, exec boil.ContextExecutor, related *MTBDeviceType) error {
	var err error

	queries.SetScanner(&o.DeviceTypeID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("device_type_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.DeviceType = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.DeviceTypeDTBLayouts {
		if queries.Equal(o.DeviceTypeID, ri.DeviceTypeID) {
			continue
		}

		ln := len(related.R.DeviceTypeDTBLayouts)
		if ln > 1 && i < ln-1 {
			related.R.DeviceTypeDTBLayouts[i] = related.R.DeviceTypeDTBLayouts[ln-1]
		}
		related.R.DeviceTypeDTBLayouts = related.R.DeviceTypeDTBLayouts[:ln-1]
		break
	}
	return nil
}

// AddLayoutDTBBlockPositions adds the given related objects to the existing relationships
// of the dtb_layout, optionally inserting them as new records.
// Appends related to o.R.LayoutDTBBlockPositions.
// Sets related.R.Layout appropriately.
func (o *DTBLayout) AddLayoutDTBBlockPositions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DTBBlockPosition) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.LayoutID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `dtb_block_position` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"layout_id"}),
				strmangle.WhereClause("`", "`", 0, dtbBlockPositionPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.Section, rel.BlockID, rel.LayoutID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.LayoutID = o.ID
		}
	}

	if o.R == nil {
		o.R = &dtbLayoutR{
			LayoutDTBBlockPositions: related,
		}
	} else {
		o.R.LayoutDTBBlockPositions = append(o.R.LayoutDTBBlockPositions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &dtbBlockPositionR{
				Layout: o,
			}
		} else {
			rel.R.Layout = o
		}
	}
	return nil
}

// AddLayoutDTBPageLayouts adds the given related objects to the existing relationships
// of the dtb_layout, optionally inserting them as new records.
// Appends related to o.R.LayoutDTBPageLayouts.
// Sets related.R.Layout appropriately.
func (o *DTBLayout) AddLayoutDTBPageLayouts(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DTBPageLayout) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.LayoutID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `dtb_page_layout` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"layout_id"}),
				strmangle.WhereClause("`", "`", 0, dtbPageLayoutPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.PageID, rel.LayoutID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.LayoutID = o.ID
		}
	}

	if o.R == nil {
		o.R = &dtbLayoutR{
			LayoutDTBPageLayouts: related,
		}
	} else {
		o.R.LayoutDTBPageLayouts = append(o.R.LayoutDTBPageLayouts, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &dtbPageLayoutR{
				Layout: o,
			}
		} else {
			rel.R.Layout = o
		}
	}
	return nil
}

// DTBLayouts retrieves all the records using an executor.
func DTBLayouts(mods ...qm.QueryMod) dtbLayoutQuery {
	mods = append(mods, qm.From("`dtb_layout`"))
	return dtbLayoutQuery{NewQuery(mods...)}
}

// FindDTBLayout retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDTBLayout(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*DTBLayout, error) {
	dtbLayoutObj := &DTBLayout{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `dtb_layout` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dtbLayoutObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from dtb_layout")
	}

	return dtbLayoutObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DTBLayout) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_layout provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbLayoutColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dtbLayoutInsertCacheMut.RLock()
	cache, cached := dtbLayoutInsertCache[key]
	dtbLayoutInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dtbLayoutColumns,
			dtbLayoutColumnsWithDefault,
			dtbLayoutColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `dtb_layout` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `dtb_layout` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `dtb_layout` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dtbLayoutPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into dtb_layout")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbLayoutMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_layout")
	}

CacheNoHooks:
	if !cached {
		dtbLayoutInsertCacheMut.Lock()
		dtbLayoutInsertCache[key] = cache
		dtbLayoutInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the DTBLayout.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DTBLayout) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dtbLayoutUpdateCacheMut.RLock()
	cache, cached := dtbLayoutUpdateCache[key]
	dtbLayoutUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dtbLayoutColumns,
			dtbLayoutPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update dtb_layout, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `dtb_layout` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dtbLayoutPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, append(wl, dtbLayoutPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update dtb_layout row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for dtb_layout")
	}

	if !cached {
		dtbLayoutUpdateCacheMut.Lock()
		dtbLayoutUpdateCache[key] = cache
		dtbLayoutUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q dtbLayoutQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for dtb_layout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for dtb_layout")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DTBLayoutSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbLayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `dtb_layout` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbLayoutPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in dtbLayout slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all dtbLayout")
	}
	return rowsAff, nil
}

var mySQLDTBLayoutUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DTBLayout) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("model: no dtb_layout provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dtbLayoutColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDTBLayoutUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dtbLayoutUpsertCacheMut.RLock()
	cache, cached := dtbLayoutUpsertCache[key]
	dtbLayoutUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dtbLayoutColumns,
			dtbLayoutColumnsWithDefault,
			dtbLayoutColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			dtbLayoutColumns,
			dtbLayoutPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("model: unable to upsert dtb_layout, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "dtb_layout", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `dtb_layout` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to upsert for dtb_layout")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == dtbLayoutMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dtbLayoutType, dtbLayoutMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "model: unable to retrieve unique values for dtb_layout")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for dtb_layout")
	}

CacheNoHooks:
	if !cached {
		dtbLayoutUpsertCacheMut.Lock()
		dtbLayoutUpsertCache[key] = cache
		dtbLayoutUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single DTBLayout record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DTBLayout) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBLayout provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dtbLayoutPrimaryKeyMapping)
	sql := "DELETE FROM `dtb_layout` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from dtb_layout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for dtb_layout")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q dtbLayoutQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no dtbLayoutQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtb_layout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_layout")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DTBLayoutSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DTBLayout slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(dtbLayoutBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbLayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `dtb_layout` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbLayoutPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from dtbLayout slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for dtb_layout")
	}

	if len(dtbLayoutAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DTBLayout) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDTBLayout(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DTBLayoutSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DTBLayoutSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dtbLayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `dtb_layout`.* FROM `dtb_layout` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dtbLayoutPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in DTBLayoutSlice")
	}

	*o = slice

	return nil
}

// DTBLayoutExists checks if the DTBLayout row exists.
func DTBLayoutExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `dtb_layout` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if dtb_layout exists")
	}

	return exists, nil
}
